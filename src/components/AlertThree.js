import React,{Component} from "react";
import { View, Text } from "react-native";

export default  class AlertThree extends Component{
    render(){
        return(
            alert("Go To Home Screen")
        )
    }
}